# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This repository is currently empty. Update this file as the project develops to document build commands, architecture, and conventions.

## Git Workflow

After completing any meaningful unit of work, commit and push to GitHub:

- Write clean, descriptive commit messages that explain *what* changed and *why*
- Push after each commit so work is never lost
- Commit granularly — one logical change per commit, not one giant commit at the end
